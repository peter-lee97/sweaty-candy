FROM node:22-alpine AS base
WORKDIR /app

FROM base AS prod
RUN apk add --no-cache curl
COPY web ./web
COPY shared ./shared
ENV NODE_ENV=production
ENV PORT=8787
ENV HOST=0.0.0.0
EXPOSE 8787
CMD ["node", "web/server.js"]
